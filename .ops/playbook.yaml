# 1op playbook — generated by `1op init`. Pointers, never secrets.
# Fill the TODOs, mirror your seeded dev users under envs.dev.accounts.users,
# and put anything sensitive in playbook.local.yaml (gitignored, never committed).
# Full contract: https://1op.dev  /  docs/SCHEMA.md
app: jde.com
repoRoot: /Users/daviderwin/dev/github.com/jde/fractal
type: Personal
description: JDE personal homepage — Next.js + three.js fractal (repo folder is `fractal`; Vercel project `fractal`).
packageManager: npm

commands:
  - { label: install, run: npm install }
  - { label: dev, run: npm run dev }     # next dev --turbopack -p 4242

envs:
  dev:
    url: http://localhost:4242   # dev script pins -p 4242, not 3000
    start: npm run dev
    accounts:
      source: inline-nonsecret
      note: no auth — personal homepage, nothing to log in to
  staging:
    url: TODO
    accounts:
      source: vault
      vaultItem: jde.com — staging
  prod:
    url: TODO   # linked Vercel project is `fractal`, but no production domain committed in the repo
    accounts:
      source: vault
      vaultItem: jde.com — production
